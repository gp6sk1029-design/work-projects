---
name: drawing-dxf
description: SolidWorks図面（.SLDDRW）をDXFに一括変換する。「DXFに変換して」「図面をDXFにして」「取引先にDXFを支給したい」「このフォルダの図面を全部DXF」などの依頼で使用する。NAS上の図面フォルダから読み取り専用で開き、原本を変更せずにDXFを別フォルダへ出力する。
---

# 図面DXF変換（SolidWorks .SLDDRW → .DXF）

加工部品を発注するとき、取引先から「**CAD（DXF）データをご支給願います**」と求められる。
その支給用DXFを、NAS上のSolidWorks図面から一括生成する。

## いつ使うか

- 見積書に「本加工部品をご注文の場合、CAD（DXF）データをご支給願います」とある
- 注文書を出した後、**発注先へ図面データを渡す**とき
- 「この図面をDXFにして」「フォルダごと全部DXFに」と言われたとき

## 前提条件（最初に必ず確認）

```bash
ls -d "/c/Program Files/SOLIDWORKS Corp"      # SolidWorksがあるか
tasklist | grep -i sldworks                    # 起動しているか
```

> ⚠️ **SolidWorksは自動起動させないこと。**
> スクリプトから起動すると初期化途中でCOM要求を受けてプロセスが落ちる。
> **ユーザーに手動で起動してもらい**、`GetActiveObject` で既に動いているインスタンスに接続する。
> 起動していなければ「SolidWorksを起動してください」と依頼する。

## 手順

### 1. 対象フォルダとSolidWorksの状態を確認
```bash
python scripts/batch_dxf.py --src "<NASの図面フォルダ>" --check
```
ファイル数・SolidWorksの接続可否を表示する（変換はしない）。

### 2. 一括変換
```bash
python scripts/batch_dxf.py --src "<NASの図面フォルダ>" --out "C:/Users/SEIGI-N13/Desktop/NTS_DXF"
```
> 出力先は**取引先名＋_DXF**（例 `NTS_DXF`）にすると、支給先ごとに整理できる。
- **原本は読み取り専用で開き、一切変更しない**
- 1件ずつログに記録し、失敗しても次へ進む
- 100件以上なら **`run_in_background: true` で実行**（1件あたり約4秒、125件で約8分半）

### 3. 検証（省略禁止）
```bash
python scripts/verify_dxf.py --src "<図面フォルダ>" --out "<DXFフォルダ>"
```
- 元図面とDXFの**件数一致**
- **中身が空のDXFがないか**（図形要素数を数える）
- ファイルができただけで終わらせない

## つまずきポイント

| 症状 | 原因と対処 |
|---|---|
| `リモート プロシージャ コールに失敗しました` | SolidWorksが起動処理中。**手動起動してから接続**する |
| `RPC サーバーを利用できません` | SolidWorksが落ちた。手動で起動し直す |
| `os.path.exists()` が False なのに `ls` では見える | **UNCパスがシェル経由でバックスラッシュを失っている**。`--src` は **`"//nas-ime5/共有名/..."` 形式（スラッシュ）で渡す**。スクリプト側でも自動復元する（`fix_path`）。ヒアドキュメント内にUNCパスを直書きしない |
| OpenDocがNoneを返す | `OpenDoc6` でエラーコードを取得して原因特定（1024=未来バージョン、32=参照不足 など） |

## 注意

- 図面の**バージョン違いフォルダ**（`...(PIP-02F02NX)` と `...(PIP-02F02NX)Ver.2`）に注意。
  同名ファイルは上書きされるので、**どちらのフォルダか必ず確認**する
- 変換後、SolidWorksは起動したままにする（図面だけ閉じる）。ユーザーが作業中の可能性があるため勝手に終了しない
- 出力DXFはAC1018（AutoCAD 2004形式）で、図面枠・寸法・文字を含む

## 自己改善ループ（AGENTS.mdに準拠）
このスキルはAGENTS.mdの方針に従い、タスク完了のたびに振り返りレポートを出力し、
SKILL.mdとMEMORY.mdを更新し続ける。ROI評価を毎回行い、費用対効果を最大化する。
